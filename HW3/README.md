# HW3
Algorithms HW3 repository

This repository contains source code written in python 3.7. It also includes two script files (dialog_script.bat and dialog_search_script.bat) to run the corresponding python programs (dialog.py and dialog_search.py). Input text files and the output text files generated by the script files are also added.
